<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gqs.mds.dao.SysDataSourceDao">

    <resultMap id="BaseResultMap" type="com.gqs.mds.entity.SysDataSource">
        <!--@Table sys_data_source-->
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="dataSourceNo" column="data_source_no" jdbcType="VARCHAR"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="driverClassName" column="driver_class_name" jdbcType="VARCHAR"/>
        <result property="url" column="url" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          id, data_source_no, username, password, name, driver_class_name, url, create_time
        from test.sys_data_source
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          id, data_source_no, username, password, name, driver_class_name, url, create_time
        from test.sys_data_source
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          id, data_source_no, username, password, name, driver_class_name, url, create_time
        from test.sys_data_source
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="dataSourceNo != null and dataSourceNo != ''">
                and data_source_no = #{dataSourceNo}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="driverClassName != null and driverClassName != ''">
                and driver_class_name = #{driverClassName}
            </if>
            <if test="url != null and url != ''">
                and url = #{url}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into test.sys_data_source(data_source_no, username, password, name, driver_class_name, url, create_time)
        values (#{dataSourceNo}, #{username}, #{password}, #{name}, #{driverClassName}, #{url}, #{createTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update test.sys_data_source
        <set>
            <if test="dataSourceNo != null and dataSourceNo != ''">
                data_source_no = #{dataSourceNo},
            </if>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="driverClassName != null and driverClassName != ''">
                driver_class_name = #{driverClassName},
            </if>
            <if test="url != null and url != ''">
                url = #{url},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from test.sys_data_source where id = #{id}
    </delete>

</mapper>